{
    "builds": [{
        "src": "store/wsgi.py",
        "use": "@vercel/python",
        "config": {"maxLanbdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "store/wsgi.py"
        }
    ]
}